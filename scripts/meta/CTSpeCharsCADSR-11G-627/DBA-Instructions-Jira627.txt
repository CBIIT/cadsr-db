DBA steps one time: 
 
 1. Run the script "JIRA627_TABLES.sql"  in SBREXT schema. 
 2. Run the script "SP_REPLACE_SPCHAR_JIRA627.sql"  in SBREXT schema
 3. Create DB JOBS according the script "create_JOBS.sql".
  We cannot test creating jobs cript becose of insufficient priveliges.
